{
  "name": "rutorrent-mobile",
  "private": true,
  "scripts": {
    "build:css": "npx @tailwindcss/cli -i ./styles/appbox-input.css -o ./appbox.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.1.0",
    "tailwindcss": "^4.1.0"
  }
}
